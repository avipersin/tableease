extends layout

block content
    div.bgempty
        div.container.h-100
            div.row.h-100.justify-content-center.align-items-center
                div.col-md-6
                    h1 Restaurant Login
                    if (message.length)
                        .alert.alert-danger #{message}
                    form(method='POST' action='/login')
                        div.form-group
                            label(for='email') Email:
                            input#email.form-control(type='email', placeholder='name@email.com' name='username', value="")
                        div.form-group
                            label(for='pw') Password:
                            input#pw.form-control(type='password' name='password', value="")
                        div.checkbox
                            label(for='remember')
                                input#remember(type='checkbox' name='remember')
                                span  Remember Me
                        button.btn.btn-primary(type='submit') Login


